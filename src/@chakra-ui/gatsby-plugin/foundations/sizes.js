import { sizes as defaultSizes } from "wildSrc/@chakra-ui/gatsby-plugin/foundations/sizes.js"

export const sizes = {
  ...defaultSizes,
  container: {
    ...defaultSizes.container,
    xl: "100em",
  },
}
